# Base LeRobot + SmolVLA
# Install first with: pip install -e ".[smolvla]"

# LoRA / PEFT fine-tuning
peft>=0.18.1

# ALOHA simulation environment
gym-aloha>=0.1.3

# Fast HuggingFace dataset downloads (avoids Xet protocol hangs)
hf-transfer>=0.1.9

# Loss curve plotting
matplotlib>=3.10.0
